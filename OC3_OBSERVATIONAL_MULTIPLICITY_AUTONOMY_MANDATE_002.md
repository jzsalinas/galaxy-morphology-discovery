# OC3 Observational Multiplicity Standing Autonomy Mandate 002

State: `PENDING_HUMAN_AUTHORIZATION`. This document creates no authorization and activates no mission.

## Scientific envelope

The mission remains to determine whether `GLOBAL_IDENTITY_WITH_ALL_VALID_VIEWS` can replace canonical-region selection while preserving independence, leakage controls, and observer-replication auditability. The first scientific stage remains `OC3-GLOBAL-VIEW-RELATION-AUDIT-001`; only its corrected prospective execution identity changes. `CROSS_OBSERVER_SOURCE_GROUPING_REQUIRES_PROSPECTIVE_CONTRACT` remains frozen.

The four outcomes remain exactly `OBSERVATIONAL_MULTIPLICITY_STRATEGY_SUPPORTED`, `OBSERVATIONAL_MULTIPLICITY_REQUIRES_GROUPING_EVIDENCE`, `OBSERVATIONAL_MULTIPLICITY_STRATEGY_INCONCLUSIVE`, and `OBSERVATIONAL_MULTIPLICITY_STRATEGY_REJECTED`.

Authority classes remain frozen project specifications/terminals, bound local DR9 brick-summary identity/geometry, bounded official DR9 documentation, and prospectively contracted grouping metadata. Budgets remain 12 requests, 16,777,216 application-body bytes, concurrency one, zero retries by default, and at most one prospectively frozen exact-resource retry.

Every action requires zero PHOTSYS reads, Tractor cells, source rows outside an explicit future contract, pixels, morphology/labels, models, embeddings, and clustering. PHOTSYS reinterpretation/V2, Panel V3 before its gate, P1, morphology learning, training, embeddings, and clustering remain prohibited.

## Human-bound first action

Policy Core V2 makes the sealed waiting state's `{path, sha256}` binding the first-action authority. A future human authorization must bind both the exact waiting-state SHA and that exact candidate path/SHA. Activation and first registration revalidate those identities. After first completion, later eligible actions remain generic under the same mandate.

The mission starts inactive with zero permits and zero consumed budget. Canonical single-use permit consumption, terminal-bound accounting, append-only lifecycle evidence, STOP, and the four terminals remain unchanged.
