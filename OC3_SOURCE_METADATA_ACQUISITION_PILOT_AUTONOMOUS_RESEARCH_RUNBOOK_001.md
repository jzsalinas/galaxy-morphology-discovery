# OC3 Source-Metadata Acquisition Pilot Autonomous Research Runbook 001

The mission begins inactive. Validate the frozen predecessor, specification, query manifest, implementation aggregate, pending mandate, waiting state and first candidate offline. A future standing authorization must bind those exact identities.

After activation, register Candidate 001, evaluate it, issue one single-use permit and execute only the sealed supervisor argv. The supervisor consumes the permit before output creation and worker launch. The worker runs the five-request state machine with exact accounting and may terminate after one or three requests when gates fail closed. No retry or resume is allowed.

Transition the observed terminal with exact request/body/retry deltas. Finalize exactly one frozen outcome, or enter `STOP_REQUIRES_HUMAN` for mandate, integrity or governance conflicts outside the frozen taxonomy. Commit compact evidence only; raw CSV response bodies and runtime output remain local.
