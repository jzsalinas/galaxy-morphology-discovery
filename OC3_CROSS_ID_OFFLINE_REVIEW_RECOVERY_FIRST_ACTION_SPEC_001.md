# OC3 Cross-ID Offline Review Recovery First Action Specification 001

Stage `OC3-CROSS-ID-OFFLINE-SEMANTIC-REVIEW-RECOVERY-001` is a prospective,
permit-gated, offline review under scope
`OFFLINE_PRIMARY_EVIDENCE_REVIEW_RECOVERY_ONLY`. It is prepared but not executed.

The action verifies the four exact local bindings in the governing specification,
runs `pdftotext -layout` once on the bound ASP PDF, parses the bound arXiv HTML,
applies `NORMALIZED_WHITESPACE_TEXT_V1` and
`UNICODE_ALNUM_TOKEN_SEQUENCE_V1` uniformly, performs the unchanged bibliographic
and A-K checks, decides Claim 9 before Claim 10, and writes only the five compact
outputs named by the governing specification.

It reserves zero requests, zero body bytes and zero retries at concurrency one.
It cannot resume, reacquire evidence, read astronomical data, choose a search
bound or threshold, perform matching, or use a semantic substitute for exact
ordered lexical-token occurrence. Execution requires a new standing human
authorization and a new single-use permit. The predecessor permit is historical
and consumed.
