# OC3 Supervised Schema-Correct Pilot-Frame Recovery Action 001

Stage `OC3-SOURCE-METADATA-PILOT-FRAME-SCHEMA-RECOVERY-001`, scope
`OFFLINE_PILOT_FRAME_SCHEMA_RECOVERY_ONLY`, action
`SUPERVISED_MEMORY_BOUNDED_FRAME_SCHEMA_RECOVERY`.

The candidate binds the same four local authorities and exact hashes, the
three frozen provider physical contracts, supervisor, corrected worker,
columnar algorithm, validator, exact supervisor argv, exact internal worker
command, diagnostic schema
`OC3_FRAME_SCHEMA_RECOVERY_EXECUTION_DIAGNOSTIC_001`, 262144-byte per-stream
capture cap, zero network/body reservations, concurrency one, zero retries and
no resume.

The supervisor consumes a new permit before real FITS access, creates output
and immutable intent first, launches one worker, records exit, signal,
resources and output evidence, validates the compact frame and writes
`TERMINAL.json`. The worker validates each full frozen header before exact
column access and uses the closed six-code error taxonomy. No historical
permit, heuristic schema lookup, network, source row, target/holdout source
information or prohibited scientific operation is available.
