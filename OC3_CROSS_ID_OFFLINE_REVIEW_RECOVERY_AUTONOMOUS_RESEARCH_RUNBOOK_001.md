# OC3 Cross-ID Offline Review Recovery Autonomous Research Runbook 001

1. Validate governing specification, mandate, manifest, closed predecessor STOP,
   exact local input hashes, first candidate and inactive state.
2. Await a new standing human authorization. While absent, evaluation returns
   `MANDATE_NOT_ACTIVE` and no permit can exist.
3. If authorized later, activate once, register the exact candidate, evaluate the
   frozen envelope and issue a fresh single-use permit.
4. The executor records permit consumption before reading evidence, verifies
   bibliographic identity, applies the frozen common lexical pipeline, writes five
   compact outputs, and reports zero firewall counters.
5. Transition the action and close at exactly one frozen outcome, or fail closed at
   `STOP_REQUIRES_HUMAN`.

No network, reacquisition, source row, matching, radius, threshold, PHOTSYS,
morphology, pixel, Panel V3, P1 or model operation is part of this runbook. Runtime
extracted text remains local and unversioned. Git history is never rewritten,
merged to main, or pushed by the mission.
