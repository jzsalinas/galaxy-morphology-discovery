# OC3 Source-Metadata Autonomous Recovery Policy Core 004

Status: **PROSPECTIVE; FROZEN FOR HUMAN REVIEW; INACTIVE**.

Run-004 is a fresh successor to the terminal Run-003. Run-003 remains immutable at
`STOP_REQUIRES_HUMAN / TECHNICAL_PATCH_GIT_DIFF_STATE_PATH_BINDING_CONFLICT` and
none of its authorization, permits, capabilities, state, ledger namespaces or
ephemeral identities is reusable.

Run-004 preserves the source-metadata scientific invariants, recovery graph semantics,
provider identities, public-anonymous rights, budgets and self-repair limits. It adds
only the fail-closed Git change partition defined by
`OC3_SOURCE_METADATA_GIT_DIFF_RUNTIME_PARTITION_AMENDMENT_001.md`.

Every repair comparison path is classified exactly once as mutable patch, validated
mission runtime, or forbidden. Only mutable patch paths enter a patch manifest.
Runtime admission requires exact path identity and cryptographic/lifecycle validation.
Any forbidden or unaccounted path stops the mission for human review.

Run-004 prospectively reuses the exact sealed Run-003 documentary evidence and its
offline transport tests. This establishes transport compatibility only. It establishes
no source row, count or scientific value. The inherited authority binds the evidence,
adapter bytes, frozen query hashes and zero-material boundary. Run-004 therefore starts
with the material acquisition action and retains its complete independent budgets.

After a distinct future human authorization, `execution_actor = AUTHORIZED_AGENT` and
`human_role = REVIEW_AND_EXPLICIT_AUTHORIZATION`. The exact command is a cryptographic
execution identity. No authorization exists while this package remains waiting.

All integrity, authority, scientific, provider, rights, budget and ambiguity stops from
Policy Core 003 remain fail-closed. Active policy mutation, recursive repair-policy
mutation, historical evidence mutation, or an unresolved binding conflict produces
`STOP_REQUIRES_HUMAN`.
