# OC3 Supervised Pilot-Frame Recovery Action 001

Stage `OC3-SOURCE-METADATA-PILOT-FRAME-RECOVERY-001`, scope `OFFLINE_PILOT_FRAME_RECOVERY_ONLY`, action `SUPERVISED_MEMORY_BOUNDED_FRAME_RECOVERY`.

The candidate binds the same four local authorities and exact hashes, supervisor, worker, columnar algorithm, validation module, exact supervisor argv, exact internal worker command, diagnostic schema `OC3_FRAME_RECOVERY_EXECUTION_DIAGNOSTIC_001`, 262,144-byte per-stream capture cap, zero network/body reservations, concurrency one, zero retries and no resume.

The supervisor consumes the new permit before real FITS access, creates the output and intent first, launches one worker, records exit/signal/resource/output evidence, validates the compact frame and writes `TERMINAL.json`. No historical permit, network, source row, target/holdout source information or prohibited scientific operation is available.
