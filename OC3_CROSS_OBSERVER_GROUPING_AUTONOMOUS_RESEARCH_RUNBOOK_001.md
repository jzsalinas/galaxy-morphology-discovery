# OC3 Cross-Observer Grouping Autonomous Research Runbook 001

All commands run from the repository root with `oc3/.venv/bin/python`. Bootstrap validation is offline.

## Before activation

1. Validate the new Policy Core, mandate, waiting state, resource manifest, first candidate, and receipt:
   `oc3/.venv/bin/python oc3/oc3_cross_observer_grouping_governor.py --validate`
2. Inspect inactive status:
   `oc3/.venv/bin/python oc3/oc3_cross_observer_grouping_governor.py --status`
3. Stop. Human review and a separate standing authorization must bind the exact waiting-state and first-candidate identities.

## Future governed lifecycle

Activation derives the first action only from state. Universal policy contains no scientific-action identity. Each material action must pass action-specific validation, registration, generic policy evaluation, immutable permit issuance, canonical permit consumption, exact runtime command verification, material execution, immutable terminal creation, and separate completion transition.

The first action may access only its exact six-resource documentary manifest, sequentially, within six requests and 12,582,912 bytes. Data Lab access is restricted to the two literal `TAP_SCHEMA` metadata queries. It cannot query `ls_dr9.tractor_n`, `ls_dr9.tractor_s`, or `ls_dr9.tractor` rows.

No bootstrap command activates the mission, issues a permit, performs network access, reads source rows, selects a radius, matches sources, or creates Panel V3/P1. Compact governance artifacts belong in Git; runtime bodies remain local. Do not push, merge main, or rewrite history.
