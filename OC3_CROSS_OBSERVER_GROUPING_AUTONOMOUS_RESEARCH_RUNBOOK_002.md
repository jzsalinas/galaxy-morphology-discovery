# OC3 Cross-Observer Grouping Autonomous Research Runbook 002

All commands run from the repository root with `oc3/.venv/bin/python`.
Preauthorization validation is offline.

## Before activation

1. Validate Policy Core V2, Mandate 002, State 002, Resource Manifest 002,
   Candidate 002, and its validation receipt with
   `oc3/.venv/bin/python oc3/oc3_cross_observer_grouping_governor.py --validate`.
2. Inspect inactive status with
   `oc3/.venv/bin/python oc3/oc3_cross_observer_grouping_governor.py --status`.
3. Stop. A separate standing human authorization must bind the exact V2 state
   and Candidate 002 identities.

## Future governed lifecycle

Activation derives the first action only from state. Each material action must
pass action-specific validation, registration, generic policy evaluation,
immutable permit issuance, single-use consumption, exact runtime-command
verification, material execution, immutable terminal creation, and a separate
completion transition.

The first action may capture only its exact six-resource manifest, sequentially,
within six requests and 12,582,912 body bytes. Every body becomes a read-only
hashed local snapshot with complete required transport evidence. Data Lab
access is restricted to the two literal `TAP_SCHEMA` metadata queries.

Successful acquisition stops at
`DOCUMENTARY_EVIDENCE_ACQUIRED_PENDING_OFFLINE_SEMANTIC_REVIEW`. A later
separately contracted offline review decides the documentary Gate from the
snapshots. Acquisition alone cannot pass that Gate or emit a mission terminal.

No preauthorization command activates the mission, issues a permit, performs
network access, reads source rows or PHOTSYS values, selects a radius, matches
sources, or creates Panel V3/P1. Runtime bodies remain local. Do not push,
merge main, or rewrite history.
