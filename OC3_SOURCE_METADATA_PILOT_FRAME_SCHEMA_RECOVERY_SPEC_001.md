# OC3 Source-Metadata Pilot-Frame Schema Recovery Specification 001

Status: **FROZEN PROSPECTIVE DESIGN; INACTIVE**

Mission: `OC3-SOURCE-METADATA-FRAME-SCHEMA-RECOVERY-AUTONOMY-001`

Scope: `OFFLINE_PILOT_FRAME_SCHEMA_RECOVERY_ONLY`

Branch: `autopilot/source-metadata-frame-schema-recovery`, based exactly on
terminal commit `507f6896dc93f5a3e3ec93b98c4a86e5a915d071`.

## Closed result and bounded review

The immediately preceding mission remains immutable at
`SOURCE_METADATA_PILOT_FRAME_RECOVERY_INTEGRITY_FAILED`. Its one authorized
worker launch returned 2 with `BOUND_IDENTITY_COLUMNS_MISSING`, first reached
`NORTH_SUMMARY`, produced no `PILOT_FRAME.json`, and materialized no target or
holdout. Its report, claim matrix, terminal state, ledger and consumed permit
are historical evidence and are never replayed.

Frozen provider contracts establish `BRICKNAME`, `BRICKID` for root and
case-sensitive `brickname`, `brickid` for regional summaries (`TTYPE1` and
`TTYPE44`). The historical observational-multiplicity implementation used
regional projection `("brickname","brickid","ra","dec","ra1","ra2","dec1","dec2")`.
The preceding supervised failure is therefore classified prospectively as
`IMPLEMENTATION_SCHEMA_CASE_MISMATCH`.

The older opaque predecessor remains `TECHNICAL_CAUSE = UNKNOWN`. Compatibility
with its uppercase implementation is not causal evidence and does not
retrospectively reclassify it.

## Unchanged scientific frame

The canonical project identity remains `(BRICKNAME, BRICKID)` after physical
decode. `GLOBAL_VIEW_BOTH`, development-fixture exclusion,
`SHA256_ASCII_GLOBAL_BRICK_IDENTITY_V1`,
`WRAP_AWARE_CLOSED_RA_BRICKROW_GUARD_1_V1`,
`HASH_ORDER_DISJOINT_GUARD_2_TARGET_2_HOLDOUT_V1`, two targets and two holdouts
remain exact. Physical provider case is representation metadata only.

The same four authorities and SHA-256 values remain bound: root
`dc943d702357f93553b9e5d15e87ace38df94eb7095f4100657407b3f9919c5f`, north
`2edd5c295fdad26852c6f224a3ff023cff43dd0e03a53acd35b767e726ee72fb`, south
`7360414f5d53571ca70fa0cb483eb8c80cfcbe80df0856a117246f442a0b9a3f`, and
development fixtures
`147c0942a5340ed18eec23a610f8afd5721390fad09c1da7ed01593deb40fab6`.

## Contract-driven physical access

Production access uses only `PRODUCTION_PHYSICAL_CONTRACTS` for
`ROOT_SUMMARY`, `NORTH_SUMMARY` and `SOUTH_SUMMARY`. Before cell access it
validates target HDU, BINTABLE, NAXIS, NAXIS1, NAXIS2, PCOUNT, GCOUNT, TFIELDS,
ordered TTYPE/TFORM and frozen absence requirements. Root decodes the exact
uppercase physical names. Regional summaries decode exactly lowercase
`brickname`, `brickid`. There is no alternate spelling, case-insensitive lookup
or runtime schema discovery. Normalization happens only while constructing the
canonical NumPy identity array.

The prospective worker taxonomy is exactly
`PHYSICAL_SCHEMA_CONTRACT_MISMATCH`, `ALLOWED_COLUMN_ACCESS_MISMATCH`,
`IDENTITY_DECODE_FAILURE`, `GLOBAL_IDENTITY_INTEGRITY_FAILURE`,
`FRAME_SELECTION_INTEGRITY_FAILURE`, and `WORKER_RUNTIME_FAILURE`.

The fixed-width NumPy architecture remains. Full lists of row dictionaries,
global per-row geometry objects and object-heavy fallback are prohibited. The
supervisor still consumes a single-use permit first, writes start intent,
launches one worker, captures bounded streams, return code, signal, time and
resource use, and writes diagnostic and terminal evidence.

## Firewall and terminal outcomes

Bootstrap opens no real FITS authority and exposes no identity value. Network,
Data Lab, source rows/counts, PHOTSYS, TYPE, DCHISQ, Sérsic, photometry,
photo-z, pixels, morphology, labels, matching, radius, threshold, object/split
group IDs, Panel V3, P1, training, embeddings and clustering remain zero.

Exactly one future terminal outcome is permitted:
`SOURCE_METADATA_PILOT_FRAME_SCHEMA_RECOVERED`,
`SOURCE_METADATA_PILOT_FRAME_SCHEMA_INTEGRITY_FAILED`, or
`SOURCE_METADATA_PILOT_FRAME_SCHEMA_RECOVERY_INCONCLUSIVE`. Bootstrap creates
no standing authorization, real permit or execution evidence.
