# Source-Metadata Acquisition Pilot Policy Core Contract 001

The Policy Core manifest freezes the governor and its CLI by exact SHA-256. While active, any Policy Core mutation yields `STOP_REQUIRES_HUMAN`. The governor validates mandate, branch, first-candidate state binding, authority classes, exact query manifest, budgets, argv receipt, firewalls, single-use permit consumption, terminal accounting and scientific finalization.

The governor cannot activate itself, create standing authorization, restore a consumed permit, raise a budget, widen authority, authorize credentials, merge main or force-push. The mission begins inactive. Every material action is registered, evaluated, permitted, consumed, executed and transitioned. Runtime evidence survives interruption; resume is false.
