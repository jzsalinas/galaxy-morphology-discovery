# Source-Metadata Descriptive Pilot Policy Core Contract 001

The manifest freezes the governor and its CLI by exact SHA-256. While the mission is active, any mismatch or mutation yields `STOP_REQUIRES_HUMAN`. The governor validates mandate, branch, first-action state binding, authority classes, literal resources, budgets, exact argv receipt, zero prohibited counters, single-use permit consumption and terminal accounting. It does not implement scientific logic or transport. It cannot activate itself, create standing human authorization, restore a consumed permit, raise a budget, widen authority, merge main, force-push, or authorize credentials.

The mission begins inactive. Human standing authorization must bind the exact waiting-state SHA, first-candidate SHA, mandate SHA and policy-core manifest SHA. Every material action is separately registered, evaluated, permitted, consumed, executed, and transitioned. Runtime evidence is preserved after interruption; resume is false.
