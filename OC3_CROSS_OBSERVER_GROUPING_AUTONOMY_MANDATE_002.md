# OC3 Cross-Observer Grouping Standing Autonomy Mandate 002

State: `PENDING_HUMAN_AUTHORIZATION`. This prospective document creates no
authorization, permit, network authority, or active mission.

## Unchanged scientific envelope

The scientific question, `SPLIT_SAFETY_GROUPING` and
`OBSERVER_REPLICATION_PAIRING` tracks, terminology, four mission outcomes,
authority classes, source-value firewall, no-radius and no-nearest rules, and
Panel V3/P1/morphology prohibitions remain exactly as frozen in the accepted
research specification.

The parent budgets remain 12 requests and 16,777,216 application-body bytes,
concurrency one, zero retries by default, and at most one exact-resource retry
only when a later candidate freezes it prospectively.

## V2 provenance correction

Live official and literature resources are preserved as exact hashed retrieval
snapshots. Upstream immutability is not assumed. The first candidate reserves
at most six requests and 12,582,912 body bytes, with zero retries, zero
redirects, and no resume.

Documentary transport may capture pages containing the text token `PHOTSYS`.
It may not read or reinterpret scientific PHOTSYS values or use PHOTSYS to
recover canonical north/south assignment.

Acquisition and semantic interpretation are distinct. The first candidate can
only acquire the six frozen snapshots and end pending a separate offline
semantic review. That later review must classify all ten required claims under
the three closed documentary outcomes before the documentary Gate is decided.

## State-bound first action

The V2 waiting state is the sole first-candidate identity authority. A future
standing authorization must bind the exact mission, V2 mandate, V2 Policy Core
manifest, V2 waiting-state path/SHA, and Candidate 002 path/SHA. Policy Core
contains no first-action identity. No standing authorization currently exists.
