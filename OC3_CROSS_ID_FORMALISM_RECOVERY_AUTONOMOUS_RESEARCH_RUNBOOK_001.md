# OC3 Cross-ID Formalism Recovery Autonomous Research Runbook 001

1. Validate the frozen policy manifest, mandate, waiting state, first
   candidate, exact argv, and zero-network preflight.
2. Stop unless a separate standing authorization binds the exact waiting state
   and first candidate.
3. On activation, register only the state-bound first action; evaluate the
   envelope; issue one SHA-derived permit; consume it exactly once.
4. Acquire only the two literal primary resources, preserving transport and
   immutable snapshot provenance. Record actual counters and stop closed on
   any mismatch, redirect, cap excess, interruption, or partial result.
5. Register a separately frozen offline semantic review. Classify claims A–K
   by epistemic layer and keep point-source and known-uncertainty assumptions.
6. Decide claim 9 before claim 10. Keep search bounds distinct from scientific
   thresholds and select neither without a later prospective contract.
7. Close with one frozen terminal outcome or enter `STOP_REQUIRES_HUMAN`.

No automatic resume, source-row access, pilot materialization, Panel V3, P1,
merge, push, or history rewrite is part of this runbook.
