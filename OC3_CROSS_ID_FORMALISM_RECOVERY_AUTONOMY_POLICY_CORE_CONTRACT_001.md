# OC3 Cross-ID Formalism Recovery Autonomy Policy Core Contract 001

The frozen policy core consists only of this contract, the governor library,
and its CLI, bound by the sealed policy-core manifest. While active, any member
mutation produces `STOP_REQUIRES_HUMAN`.

The governor requires an exact standing authorization bound to the current
waiting-state file and exact first candidate. Each epistemically material
action requires prior registration, an exact candidate argv hash, a
prospectively validated action receipt, and a SHA-derived single-use permit.
Permit consumption precedes network construction. Replays and concurrent
pending actions fail closed. Actual request/body/retry counters, rather than
reservations, debit the append-only state ledger.

The parent envelope is four requests, 8,388,608 response-body bytes,
concurrency one, and zero retries. It admits only frozen project terminals and
specifications plus primary cross-identification literature. It forbids
astronomical data, source rows, morphology, labels, models, Panel V3, P1,
threshold selection, merging to `main`, force-push, secrets, and runtime-heavy
Git artifacts.

Autonomy continues only until a frozen scientific terminal or
`STOP_REQUIRES_HUMAN`. Bootstrap creates no authorization and no permit.

