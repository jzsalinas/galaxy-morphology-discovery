# OC3 Source-Metadata Autonomous Recovery Mutable Adapter Finding 004

`MUTABLE_ADAPTER_STATIC_SHA_CONTRADICTION` is the closed preauthorization finding at reviewed base commit `c92270ecb0746da6ee1a3711e7bc36f7184566d2`.

Technical Authorities Registry V1 bound `transport_registry.py` by its current SHA while the Mutable Technical Surface explicitly allowed agentic repair of that file. Static preflight would therefore reject a legitimate repair before its patch evidence could be evaluated.

Registry V2 freezes adapter identity, permitted mutable path and eligible official evidence. The bootstrap SHA remains verifiable against the reviewed historical Git blob but is not compared with current mutable bytes. Successful repair independently computes and persists the active implementation SHA; the material candidate binds it, and the executor rechecks it before network access.

The prospective TAP adapter identity is registered without an implementation or validated contract. Registration permits bounded consideration only and implies no TAP behavior or validity. Scientific Invariants and Recovery Graph remain unchanged.
