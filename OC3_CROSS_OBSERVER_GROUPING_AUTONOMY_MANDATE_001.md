# OC3 Cross-Observer Grouping Standing Autonomy Mandate 001

State: `PENDING_HUMAN_AUTHORIZATION`. This document creates no authorization, permit, network authority, or active mission.

## Scientific envelope

The mission asks what morphology-independent evidence can support conservative split safety and, separately, same-object north/south observer-replication pairing. `CATALOG_SOURCE_OBSERVATION`, `CROSS_OBSERVER_ASSOCIATION`, `SPLIT_GROUP_ID`, and `ASTROPHYSICAL_OBJECT_GROUP` remain distinct. The research specification and the closed observational-multiplicity terminal define the scientific boundary.

The four outcomes are exactly `CROSS_OBSERVER_GROUPING_SUPPORTED_FOR_SPLITS_AND_REPLICATION`, `CROSS_OBSERVER_SPLIT_SAFETY_SUPPORTED_PAIRING_UNRESOLVED`, `CROSS_OBSERVER_GROUPING_EVIDENCE_INCONCLUSIVE`, and `CROSS_OBSERVER_GROUPING_STRATEGY_REJECTED`.

Authority classes are frozen project terminals/specifications, official Legacy Survey DR9 documentation, official NOIRLab Data Lab documentation, primary cross-identification literature, bound local global-view evidence, prospectively contracted source metadata, and prospectively contracted validation anchors.

The parent budgets are 12 requests and 16,777,216 application-body bytes, concurrency one, zero retries by default, and at most one exact-resource retry only when a later candidate freezes it prospectively. Every action starts with zero PHOTSYS, forbidden source fields, source rows outside an explicit later contract, pixels, morphology, labels, models, training, embeddings, clustering, Panel V3, and P1.

## State-bound first action

The waiting state is the only first-candidate identity authority. A future standing authorization must bind the exact mission, mandate, Policy Core manifest, waiting-state path/SHA, and first-candidate path/SHA. No standing authorization exists in bootstrap.

The first candidate is the bounded documentary-feasibility stage. It may later retrieve only its literal six-resource manifest after authorization and a single-use permit. It may query only `TAP_SCHEMA` metadata at Data Lab and cannot read scientific source rows.

After activation, eligible actions require exact command binding, action-specific validation, bounded reservations, immutable SHA-derived permit consumption, append-only lifecycle evidence, and closed terminal accounting. Any Policy Core mutation while active, authority expansion, credentials, uncontracted astronomical values, morphology work, Panel V3, or P1 requires `STOP_REQUIRES_HUMAN`.
