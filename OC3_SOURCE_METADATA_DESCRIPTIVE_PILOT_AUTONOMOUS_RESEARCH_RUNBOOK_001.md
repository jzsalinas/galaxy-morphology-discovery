# Source-Metadata Descriptive Pilot Autonomous Research Runbook 001

1. Verify exact branch, closed predecessor, policy core, mandate, waiting state, first candidate and validation receipt.
2. Obtain a separate standing human authorization bound to that exact waiting state. Bootstrap does not create it.
3. Activate; register the state-bound offline frame action; evaluate; issue and consume one permit; execute exact argv; transition using observed zero network/body/retry deltas.
4. Review `PILOT_FRAME.json`. Only after valid completion may code generate a separate prospective five-request candidate with literal schema/count/row queries derived from the frozen target guard union. Holdouts remain absent.
5. Register, permit and consume before public transport. Enforce schema then both counts then rows; close resource-bound without substitution when either count exceeds 150000. Preserve partial evidence after interruption and stop; never replay a consumed permit.
6. Validate rows, compute only K=2 local spherical topology, publish compact aggregates, and close at exactly one terminal.

At every boundary, fail closed on hash, identity, projection, order, duplicate, cap, credential, source-value, holdout, matching, radius, threshold, group-ID, morphology, Panel V3, P1 or accounting violations. Never commit source bodies or per-anchor tables.
